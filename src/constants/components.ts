export enum Sizes {
    SMALL = 'small',
    MEDIUM = 'medium',
    LARGE = 'large',
}

export enum Animations {
    NONE = 'none',
    DARKEN = 'darken',
    SWAP = 'swap',
}

export enum Direction {
    HORIZONTAL = 'horizontal',
    VERTICAL = 'vertical'
}